# Maintainer: Vilmar Catafesta <vcatafesta@gmail.com>

pkgname=voidbr-mango
pkgdesc="VoidBR MANGO Meta Package"
pkgver=$(date +%Y%m%d)
pkgrel=$(date +%H%M)
provides=()
replaces=()
arch=('any')
license=('MIT')
#url="ssh://git@github.com/voidlinuxbr/${pkgname}"
url="https://github.com/voidlinuxbr/${pkgname}"
source=("git+${url}.git")
md5sums=('SKIP')
makedepends=()
depends=(
  mangowc                 # Dwl-based wayland compositor using scenefx
	swaybg                  # Wallpaper tool for Wayland compositors
  sddm                    # QML based X11 display manager
  voidbr-sddm-themes      # sddm Themes for VoidLinuxBR
  #weston                 # Reference implementation of a Wayland compositor
  #
  wlogout                 # Wayland based logout menu
 	Waybar                  # Barra de status customizável para Wayland
	wofi                    # Lançador de aplicativos e menu de busca
	#rofi                   # Window switcher, run dialog and dmenu replacement
  qt5-wayland             # Cross-platform application and UI framework (QT5) - Wayland component
  qt6-wayland             # Cross-platform application and UI framework - Wayland
	xorg-server-xwayland
  xdg-desktop-portal-wlr
	xdg-desktop-portal      # Camada de comunicação entre apps e o desktop
	xdg-desktop-portal-gtk  # Implementação GTK do portal XDG
	xdg-user-dirs           # Tool to help manage user directories
	xdg-user-dirs-gtk       # GTK+ tool to help manage user directories
	lua55
	mpv                     # Video player based on MPlayer/mplayer2
  #
	cliphist                # Histórico da área de transferência
  #dunst                  # Lightweight and customizable notification daemon
	mako                    # Servidor de notificações Wayland
	grim                    # Utilitário para captura de tela (screenshot)
	slurp                   # Seleção de área para captura de tela
	swappy                  # Wayland native snapshot editing tool, inspired by Snappy on macOS
	wl-clipboard            # Ferramentas de clipboard (wl-copy/wl-paste)
	brightnessctl           # Controle de brilho via linha de comando
	playerctl               # MPRIS command-line controller and library
	#
	dbus                    # Message bus system
	elogind                 # Gerenciador de login e sessões (seat management)
	foot                    # Terminal leve e otimizado para Wayland
	#qView                  # Image viewer designed with minimalism and usability in mind
	eom                     # Visualizador de imagens (Eye of MATE)
	fuse-exfat              # Suporte para montagem de sistemas exFAT
	ffmpeg                  # Ferramentas para manipulação de áudio e vídeo
	mesa-dri                # Drivers gráficos (Direct Rendering Infrastructure)
	mesa-vaapi              # Aceleração de vídeo via hardware (VA-API)
	psmisc                  # Utilitários de processo (killall, fuser, pstree)
	cpupower                # Ferramenta de gerenciamento de frequência da CPU
	noto-fonts-ttf          # Fontes Noto da Google
	font-awesome            # Fontes de ícones para barras e interfaces
	optipng                 # Otimizador de compressão para arquivos PNG
	sassc                   # Compilador CSS (SASS)
	jq                      # Processador de arquivos JSON via CLI
	swaylock               # Bloqueador de tela para Wayland
	swayidle                # Gerenciador de inatividade (idle) e bloqueio
	sway-audio-idle-inhibit # Inibe o bloqueio/hibernação durante reprodução de áudio
	#wmenu                   # Menu de seleção estilo dmenu/wofi para Wayland
	xdg-utils               # Ferramentas de integração com a área de trabalho
	#xdg-desktop-portal-wlr  # Implementação WLR (Sway) do portal XDG
	rtkit                   # Gerenciamento de prioridade de tempo real
	mate-polkit             # Agente de autenticação para permissões root
	mate-calc               # Calculadora do ambiente MATE
	kanshi                  # Gerenciador dinâmico de monitores
	Thunar                  # Gerenciador de arquivos (XFCE)
	thunar-archive-plugin   # Integração de compactação no Thunar
  gvfs                    # Suporte a dispositivos locais e remotos
  udisks2                 # Gerenciamento de discos
  tumbler                 # Miniaturas no Thunar
	engrampa                # Gerenciador de arquivos compactados
	pluma                   # Editor de texto leve (MATE)
	nwg-look                # Configurador de temas GTK para Wayland
	atril                   # Visualizador de documentos (PDFs)
	pipewire                # Servidor de áudio moderno
	wireplumber             # Gerenciador de sessão para PipeWire
	pavucontrol             # Volume control applet for Pipewire
	alsa-pipewire           # Server and user space API to deal with multimedia pipelines - ALSA client library
	pulseaudio-utils        # Featureful, general-purpose sound server - utilities
	libspa-bluetooth        # Server and user space API to deal with multimedia pipelines - bluetooth plugins
	bluez                   # Bluetooth tools and daemons
	blueman                 # GTK+ Bluetooth Manager
	libjack-pipewire        # Server and user space API to deal with multimedia pipelines - JACK client library
	alsa-plugins-pulseaudio   # Advanced Linux Sound Architecture (ALSA) extra plugins - Pulseaudio output plugin
	alsa-utils                # Advanced Linux Sound Architecture (ALSA) utilities
	xfce4-terminal            # Modern terminal emulator primarly for the Xfce desktop environment
	NetworkManager            # Gerenciador de rede (daemon)
	network-manager-applet    # Ícone de bandeja para controlar conexões
	voidbr-base-config        # Configuração base personalizada
	voidbr-mango-config       # Configuração base personalizada
	voidbr-wallpapers         # Wallpapers for Void Linux BR
	voidbr-tokyo-theme        # TokyoNight-Dark theme with icons, cursors, and wallpaper
  voidbr-dracula-theme      # Tema visual Dracula aplicado
  voidbr-professional-theme
  voidbr-xfwm-axiom-theme
  voidbr-beauty-icons
	firefox                   # Mozilla Firefox web browser
	firefox-i18n-pt-BR        # Firefox Portuguese (Brazilian) language pack
	# nerd-fonts              # Iconic font aggregator collection
	ttf-jetbrains-mono-nerd   # Patched font JetBrains Mono from nerd fonts library
	lm_sensors                # Utilities to read temperature/voltage/fan sensors
	acpi                      # Informations about ACPI devices (battery, thermal sensors and power)
	acpid                     # ACPI Daemon (acpid) With Netlink Support
  htop                      # Interactive process viewer
  noto-fonts-emoji          # Google Noto Emoji Fonts
  nwg-bar                   # GTK3-based button bar for wlroots-based compositors
)

package() {
	# Verify default folder
	if [ -d "${srcdir}/${pkgname}/${pkgname}" ]; then
		InternalDir="${srcdir}/${pkgname}/${pkgname}"
	else
		InternalDir="${srcdir}/${pkgname}"
	fi

	cd "${srcdir}/${pkgname}"
	# Copiar a estrutura de /etc, /usr e /opt para /
	local dirs=("boot" "etc" "opt" "usr")
	for dir in "${dirs[@]}"; do
		if [ -d "${InternalDir}/${dir}" ]; then
			cp -a "${InternalDir}/${dir}" "${pkgdir}/"
		fi
	done

	# Install license file if present
	if [ -f "LICENSE" ]; then
		install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
	fi

	# Install documentation if present
	if [ -f "README.md" ]; then
		install -Dm644 README.md "${pkgdir}/usr/share/doc/${pkgname}/README.md"
	fi
}

pre_install() {
	log "Iniciando instalação"
}

post_install() {
	log "Ativando serviços"

	ln -sf /etc/sv/dbus /var/service/ 2>/dev/null || true
	ln -sf /etc/sv/elogind /var/service/ 2>/dev/null || true
	ln -sf /etc/sv/NetworkManager /var/service/ 2>/dev/null || true
	ln -sf /etc/sv/rtkit /var/service/ 2>/dev/null || true

	log "Finalizando instalação"
}

pre_upgrade() {
	log "Iniciando upgrade"
}

post_upgrade() {
	log "Finalizando upgrade"
}
